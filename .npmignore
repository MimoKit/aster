# 发布到 npm 时排除的内容
#
# 注意：src/ 与 Cargo.toml 必须**随包分发**——
# npm 上没有预编译二进制时，首次运行会用它们就地 cargo build。
# package.json 的 files 字段是白名单，这里做二次兜底。

# Rust 构建产物（体积大，不入包）
target/
**/*.rs.bk

# Node 侧开发文件
test/
node_modules/
package-lock.json
pnpm-lock.yaml
yarn.lock
scripts/

# 运行期数据（绝不入库/入包）
config.toml
aster-data/
*.pid
logs/
*.log
*.tgz

# 编辑器与系统文件
.git/
.gitignore
.idea/
.vscode/
*.swp
.DS_Store
